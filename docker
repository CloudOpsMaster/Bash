# Docker bash

git clone https://github.com/
docker build -t projectname .
docker tag <container-id> username/projectname
docker login (successful)
docker push username/projectname

# useful

docker image
docker ps
docker run -p 8080:80 username/projectname
docker build -t username/projectname .
docker stop c1248..........


# Ubuntu
apt apdate
apt install docker.io
docker --version
docker run -p 8080:80 username/projectname

